*************************************	Homework 4	******************************************************

1. (a) MyWordCount.java is the Java File. Other files submitted include two shell scripts testing both scenarios of case sensitive on and
case sensitive off along with the output files.
   (b) eng_stopwords.txt is also included. This is the file which the program checks for removing stopwords.
   (c) Invalid command line arguments have been checked for.
   (d) Remove Punctuations and Stopwords handled by different methods.
   (e) Tool Interface has been implemented.

2. (a) AnalyzeStock.java is the Java File. Other files submitted include nine shell scripts testing the nine possible scenarios along with
the corresponding output files. The output is sorted as requested.
   (b) All nine scenarios have been tested and working as expected with the stockdatafull.csv file.
   (c) There were null cells in the excel file, which have been handled inside the program.
   (d) Tool interface has been implemented.
   (c) The rounding off of the values has been done by a custom method that takes the decimal places to be rounded off and rounds off
accordingly. This is set to 2 currently inside the program to comply with the output mentioned.

***************************************************************************************************************
